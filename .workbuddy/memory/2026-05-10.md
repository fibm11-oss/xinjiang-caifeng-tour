# 2026-05-10 工作記錄

## 網站優化任務（@scene#16: "網站開發"）

### 完成項目

#### 1. 建立 SVG 圖片檔案（共 16 個）

**Hero 背景圖：**
- `images/hero-xinjiang.svg` — 首頁 Hero 背景（天山雪山+賽里木湖）
- `images/hero-car.svg` — 包車服務頁面 Hero 背景
- `images/hero-routes.svg` — 推薦行程頁面 Hero 背景

**景點風景圖（images/scenic/）：**
- `sayram-lake.svg` — 賽里木湖
- `nalati.svg` — 那拉提草原
- `kashgar.svg` — 喀什古城
- `tianchi.svg` — 天山天池
- `duku.svg` — 獨庫公路
- `karakul.svg` — 喀拉庫勒湖
- `taklamakan.svg` — 塔克拉瑪干沙漠
- `bazaar.svg` — 國際大巴紮

**行程卡片圖（images/tours/）：**
- `tour1-ili-10days.svg` — 伊犁河谷自駕10日遊
- `tour2-grassland.svg` — 伊犁草原8日深度之旅
- `tour3-south-xj.svg` — 南疆絲路秘境9日
- `tour4-kanas.svg` — 北疆喀納斯8日

**網站圖示：**
- `images/favicon.svg` — 網站 SVG 圖示（紅底白字「疆」）

#### 2. 更新 CSS 樣式（css/style.css 全部重寫）
- 新增 CSS 變數（--primary, --secondary, --accent 等）
- 優化響應式斷點（1024px / 768px / 480px）
- 加入卡片懸停動畫（translateY + box-shadow）
- 加入 `fadeInUp` 淡入動畫 keyframes
- 優化 `.page-hero::after` 漸層過渡效果
- 統一圓角（--radius: 10px, --radius-lg: 16px）
- 加強陰影層級（--shadow / --shadow-hover / --shadow-lg）

#### 3. 更新 HTML 檔案

**index.html（首頁）：**
- 行程卡片（.tour-card）改為使用 `images/tours/*.svg` 背景圖
- 加入 `<link rel="icon">` 指向 favicon.svg
- 加入 `scroll-behavior: smooth` 與淡入動畫 CSS
- 加入「回到頂部」按鈕（#backToTop）
- 加入 IntersectionObserver 捲動淡入效果

**routes.html（推薦行程）：**
- 四個行程詳情區塊改為 `<img>` 顯示行程圖
- 更新 `.page-hero` 背景為 `hero-routes.svg`
- 加入 favicon、回到頂部按鈕

**car-rental.html（包車服務）：**
- 更新 `.page-hero` 背景為 `hero-car.svg`
- 加入 favicon、回到頂部按鈕

**contact.html（聯絡我們）：**
- 更新 `.page-hero` 背景為 `hero-xinjiang.svg`
- 加入 favicon、回到頂部按鈕

### 技術備註
- 所有圖片使用 SVG 格式，無需外部依賴，可離線正常使用
- HTML 中圖片路徑使用相對路徑（如 `images/tours/tour1-ili-10days.svg`）
- CSS 中 Hero 背景使用 `url('../images/...')` 語法
- 回到頂部按鈕使用 `window.scrollY > 400` 觸發顯示

### 待後續改進
- [ ] 可替換為高畫質實景照片（目前為 SVG 矢量示意圖）
- [ ] 可加入更多頁面內互動效果（如圖片燈箱）
- [ ] 可優化手機版 `.page-hero` 文字大小

---

## 網站修正與優化（下午）

### 完成項目

#### 1. 首頁新增景點（index.html）
在「為什麼選擇新疆采風包車？」區塊新增：
- **喀納斯湖**：北疆明珠，湖光山色四季皆美，秋季白樺林金黃璀璨
- **禾木村**：「神的自留地」，原始圖瓦人村落，晨霧繚繞、木屋錯落

#### 2. 更新 SVG 圖片字體（共 12 個檔案）
將所有 SVG 中的字體從 `Arial` 改為支援中文的清晰字體堆疊：
```css
'Microsoft YaHei','PingFang TC','Noto Sans TC',sans-serif
```
修改檔案：
- `images/tours/*.svg`（4個行程圖）
- `images/scenic/*.svg`（8個景點圖）
- 同時將簡體中文轉換為繁體中文

#### 3. 修改七座商務車適合人數（car-rental.html）
- 標題改為「7座商務車（適合4-5人舒適遊）」
- 兩個車型的人數標籤改為「4-5人」
- 行李標籤改為「4-5件行李」

#### 4. 修改包車費用說明（car-rental.html）
- 標題：不含司機食宿 → **含司機食宿** ✅
- 說明：長途行程需補貼司機食宿，**費用已包含**

#### 5. 聯絡資訊微信改為 LINE（所有 HTML）
- index.html、car-rental.html、routes.html：頁尾聯絡資訊改為「📱 LINE：XJCaifeng」
- contact.html：
  - 「微信掃碼諮詢」→「LINE 諮詢」
  - 「聯絡微信」表單欄位改為「LINE ID」
  - 所有相關說明文字都已更新

---

## 行動裝置優化（下午）

### 完成項目

#### 1. CSS 響應式設計升級（css/style.css）
- **完整重寫響應式區塊**：
  - `1024px` 以下：特色網格改為單欄、景點網格改為2欄、行程網格改為單欄
  - `768px` 以下：加入手機版選單樣式（.mobile-menu-btn）、行程詳情改為單欄、包車服務改為單欄、聯絡我們改為單欄
  - `480px` 以下：完整響應式（導航、Hero、頁首圖、卡片、表格、按鈕）
  - `360px` 以下：極小螢幕優化

- **新增手機版選單按鈕樣式**：
  ```css
  .mobile-menu-btn {
      display: none;
      flex-direction: column;
      gap: 5px;
      /* 漢堡選單三條線動畫 */
  }
  ```

- **表格響應式優化**：
  - 加入 `-webkit-overflow-scrolling: touch`
  - 表格最小寬度設為 `600px`

- **表單響應式優化**：
  - 手機版表單欄位改為單欄排列
  - 字體大小設為 `16px`（防止 iOS 自動縮放）

- **圖片優化**：
  - 確保所有圖片 `max-width: 100%`
  - 加入 `height: auto` 保持比例

- **回到頂部按鈕行動優化**：
  - `480px` 以下：按鈕縮小為 `44x44px`，位置調整為 `bottom:20px; right:20px`

- **動畫效能優化**：
  - 加入 `prefers-reduced-motion: reduce` 媒體查詢，尊重使用者減少動畫設定

#### 2. HTML 導航列加入手機版選單按鈕
更新所有 4 個 HTML 檔案的 `<nav>` 區塊：
```html
<button class="mobile-menu-btn" onclick="toggleMenu()" aria-label="選單">
    <span></span>
    <span></span>
    <span></span>
</button>
<ul class="nav-menu" id="navMenu">
    <!-- 選單項目 -->
</ul>
```

修改檔案：
- `index.html`
- `routes.html`
- `car-rental.html`
- `contact.html`

#### 3. JavaScript 加入手機選單功能
所有 HTML 檔案的 `<script>` 區塊加入：
```javascript
// 手機版選單切換
function toggleMenu() {
    var menu = document.getElementById('navMenu');
    var btn = document.querySelector('.mobile-menu-btn');
    if (menu && btn) {
        menu.classList.toggle('active');
        btn.classList.toggle('active');
    }
}

// 點擊選單連結後自動關閉選單
document.addEventListener('DOMContentLoaded', function() {
    var menu = document.getElementById('navMenu');
    if (menu) {
        var links = menu.querySelectorAll('a');
        links.forEach(function(link) {
            link.addEventListener('click', function() {
                menu.classList.remove('active');
                var btn = document.querySelector('.mobile-menu-btn');
                if (btn) btn.classList.remove('active');
            });
        });
    }
});
```

修改檔案：
- `index.html`：加入選單功能 + 回到頂部功能 + 淡入動畫
- `routes.html`：加入選單功能 + 回到頂部功能 + 淡入動畫
- `car-rental.html`：加入選單功能 + 回到頂部功能
- `contact.html`：加入選單功能 + 回到頂部功能

### 技術備註
- 手機版選單使用 CSS `classList.toggle()` 切換顯示/隱藏
- 選單按鈕使用三條線（span）組成漢堡圖示
- 選單展開時，按鈕會變為 X 圖示（使用 CSS transform 旋轉）
- 點擊選單連結後會自動關閉選單（DOMContentLoaded 事件監聽）
- 表格使用水平捲動（overflow-x: auto）在小螢幕上檢視
- 表單欄位字體設為 16px 防止 iOS Safari 自動縮放

### 測試建議
- [ ] 在手機實機測試選單開關
- [ ] 測試表格在 480px 以下是否可以水平捲動
- [ ] 測試表單在手機上的輸入體驗
- [ ] 測試圖片是否正確縮放
- [ ] 測試回到頂部按鈕在行動裝置上的顯示

### 檔案異動清單
- `css/style.css` — 響應式設計升級（新增 361 行）
- `index.html` — 加入手機選單按鈕 + JavaScript
- `routes.html` — 加入手機選單按鈕 + JavaScript
- `car-rental.html` — 加入手機選單按鈕 + JavaScript
- `contact.html` — 加入手機選單按鈕 + JavaScript
